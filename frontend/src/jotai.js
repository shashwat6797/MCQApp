import { atom } from "jotai";

export const accessTokenAtom = atom(null);
export const isAuthenticatedAtom = atom(false);
export const usernameAtom = atom(null);
export const useremailAtom = atom(null);
